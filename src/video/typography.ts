import {loadFont} from '@remotion/google-fonts/BebasNeue';

export const {fontFamily: displayFont} = loadFont('normal', {
  weights: ['400'],
  subsets: ['latin'],
});

export const bodyFont = 'Arial, Helvetica, sans-serif';
